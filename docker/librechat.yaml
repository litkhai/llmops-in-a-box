# GENERATED by scripts/stack.sh render — DO NOT EDIT. Edit stack.yaml and re-run `./scripts/stack.sh render`.
# profile: phase-4

version: 1.2.8
cache: true

endpoints:
  custom:
    - name: "Sovereign AI Stack"
      apiKey: "${LITELLM_MASTER_KEY}"
      baseURL: "http://litellm:4000/v1"
      models:
        default:
          - "auto"
        fetch: false
      titleConvo: true
      titleModel: "gpt-4o"
      modelDisplayLabel: "Sovereign AI Stack"
      iconURL: ""

# Model catalog (from stack.yaml):
#   qwen-7b        Qwen2.5 7B — GPU — Runs on a RunPod GPU pod. No data leaves the boundary.
#   qwen-0.5b      Qwen2.5 0.5B — CPU — Runs on EC2 CPU. No GPU required, no data leaves the boundary.
#   gpt-4o         GPT-4o — OpenAI commercial API.
#   claude-sonnet  Claude Sonnet 4.5 — Anthropic commercial API, format-translated by LiteLLM.
